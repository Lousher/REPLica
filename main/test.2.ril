(import (replica) (loader) (directive))
(let ()
  (texture night "../assets/bg/yuwen.bedroom.night.png")
  (texture light "../assets/bg/yuwen.bedroom.night.light.png")

  (*actions*
   (list
    (make-animation (static night))
    (make-animation (static light))
    (jump "test.1.so")))
  )
