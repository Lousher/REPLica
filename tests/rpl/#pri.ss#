; executed in root folder
(library-directories "main/")
(import (rpl eval))

(define test-scripts
  '(
    (bundle "tests/rpl/pri.rpk")
    (assets
     (texture bgp "avenue")
     (texture bt "tab"))

    (at 500 500
	(parallel
	 (origin 0.5 0.0
	     (anchor 0.0 0.0
		     (scale 0.5
			    (show bgp))))
     (at 200 200 (scale 0.5 (show bt)))))
    
    ))

(render test-scripts)
